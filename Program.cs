using System;

namespace FileSystemSnippets
{
    class Program
    {
        static void Main(string[] args)
        {
            // DirGetFileSystemEntries.Launch();
            // DriveInfoSnippet.Launch();
            // GetFullPathSnippet.Launch();
            // GetFileNameUsage.Launch();
            // DriveInfoSnippet.Launch();
            // DirInfoGetFilesGlob.Launch();
            // GetPathRootSnippet.Launch();
            // IsPathRootedUsage.Launch();
            // GetFileAttributes.Launch();
            // FileInfoUsage.Launch();
            // PathCombineSnippet.Launch();
            // SpecialFolders.Launch();
            // GetFileTimes.Launch();
            // GetFileOwner.Launch();
            // CopyFileSnippet.Launch();

            Console.ReadLine();
        }
    }
}